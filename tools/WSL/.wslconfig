[wsl2]
# WSL2が使用する最大メモリ。ホストの半分(8GB)に制限し、Windows側の安定性を確保。
memory=8GB
# 使用するCPUコア数。4コア割り当てることでビルド速度を向上。
processors=4
# メモリ不足時の退避領域。物理メモリの負荷を抑えるため2GBに設定。
swap=2GB
# ホストから localhost:ポート でコンテナにアクセス可能にする。
localhostForwarding=true
# 【重要】WSL2内の未使用メモリをWindowsに自動返却し、メモリ肥大化を防止。
autoMemoryReclaim=gradual
